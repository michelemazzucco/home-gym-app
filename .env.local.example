# OpenAI API Key
OPENAI_API_KEY=your_openai_api_key_here

# Set to true to run the app without OpenAI. The API routes return canned data,
# so you can click through all three steps without spending credits.
MOCK_OPENAI=false
